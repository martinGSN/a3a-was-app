server:
  port: 8080

spring:
  application:
    name: user-product-app

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      probes:
        enabled: true
      show-details: never
